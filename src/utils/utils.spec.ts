import { expect } from 'chai';
import { describe } from 'mocha';

// import { MainClass, SubClass } from '../constants/constants';
// import {
//   getNumberOfMainClasses,
// } from './utils';

describe('getNumberOfMainClasses Function', () => {
  const result = 'hi';
  expect(result).to.equal('hi');
  // describe('Should get no results', () => {
  //   const result = getNumberOfMainClasses([]);
  //   expect(result).to.equal(0);
  // });
  //
  // describe('Should get one Result', () => {
  //   const result = getNumberOfMainClasses([MainClass.Acolyte]);
  //   expect(result).to.equal(1);
  // });
  //
  // describe('Should also get one Result', () => {
  //   const result = getNumberOfMainClasses([MainClass.Archer, SubClass.Blacksmith, SubClass.Priest]);
  //   expect(result).to.equal(1);
  // });
});
